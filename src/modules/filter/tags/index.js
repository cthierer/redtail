/**
 * @module redtail/modules/filter/tags
 */

/**
 * Mapping of identifiers to Riot tags mountable by this module.
 * @type {object}
 */
const tags = {
  match: 'filter-match',
  pagination: 'filter-pagination',
  sorter: 'filter-sorter'
}

export default { tags }
export { tags }
